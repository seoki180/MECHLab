## Using CPython 3.13.5 interpreter 

Creating virtual environment at: .venv

Activate with:

```bash
  pip install uv #필요시
  uv run app.py #dependency 자동 생성 및 실행
```

## Dependency
    python = ">=3.13",
    "wxpython>=4.2.4",
